import React from 'react';

const NoPhoto = props => (
  <li className="not-found">
    <h3>No Results Found</h3>
    <p>Your search did not return any results. Please try something else.</p>
  </li>
)

export default NoPhoto;
